import SimplePage from '@/components/SimplePage';

export default function ContactPage() {
  return (
    <SimplePage
      active="contact"
      eyebrow="Contacto"
      title="Escribinos"
      intro="hola@latamesa.co.uk — London, UK. Para colaboraciones editoriales, curatoriales o de prensa."
    />
  );
}
